{
  "name": "agromarkethub",
  "version": "1.0.0",
  "description": "AgroMarketHub - Farmers Direct-to-Consumer Marketplace with AI Demand Forecasting",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:backend": "npm run dev --workspace=backend",
    "dev:ai": "cd ai-service && python -m uvicorn main:app --reload --port 8000",
    "dev:all": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\" \"npm run dev:ai\"",
    "install:all": "npm install && npm install --workspace=frontend && npm install --workspace=backend",
    "install:ai": "cd ai-service && pip install -r requirements.txt",
    "install:ai-ml": "cd ai-service && pip install --default-timeout=1000 -r requirements-ml.txt"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}

