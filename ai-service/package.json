{
  "name": "agromarkethub-ai-service",
  "version": "1.0.0",
  "description": "AgroMarketHub AI Service - FastAPI for demand forecasting",
  "private": true,
  "scripts": {
    "dev": "python -m uvicorn main:app --reload --port 8000",
    "start": "python -m uvicorn main:app --host 0.0.0.0 --port 8000",
    "install": "pip install -r requirements.txt"
  },
  "engines": {
    "python": ">=3.9"
  },
  "dependencies": {
    "react-icons": "^5.5.0"
  }
}
